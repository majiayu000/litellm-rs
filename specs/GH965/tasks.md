# Task Plan

## Linked Issue

GH-965 / #965

## Spec Packet

- Product: `product.md`
- Tech: `tech.md`

## 实现任务

- [ ] `SP965-T001` Covers: B-001, B-002, B-003, B-004, B-005, B-006, B-007, B-008, B-009, B-010, B-011, B-012. Owner: maintainer decision owner + spec coordinator. Dependencies: current packet merged；issue comment `4982855807`. Done when: `HD-001` 至 `HD-004` 均为 resolved，记录 exact runtime/default binding、validated request context、现有 `ErrorCode`/`CanonicalError` 的 exhaustive typed mapping、0.6.0 deprecation → 0.7.0 removal、release-workflow prerequisite、rollback 与更新后的 tranche scope；spec amendment 通过独立 review 并合并，此前后续 implementation 明确 blocked. Verify: 人工核对无 `unresolved`、无新增平行 error taxonomy；`python3 <specrail>/checks/check_workflow.py --repo <specrail> --spec-dir "$PWD/specs/GH965"`；`git diff --check`；independent spec review。
- [ ] `SP965-T002` Covers: B-001, B-003, B-004, B-007, B-008, B-009, B-010. Owner: runtime-contract owner. Dependencies: SP965-T001 merged. Done when: D1 在批准 API 下使 provider construction、deployment selection/execution、retry/fallback、lease/state 与 immutable generation 属于同一 runtime contract；`RuntimeHandle` 在 bind 时 pin 住 `Arc<RoutingSnapshot>` 并只经 `snapshot()` 暴露只读视图，不暴露 `&Arc<UnifiedRouter>`；不透明 `RuntimeBinding` token 持有 router owner，process default 原子存 token 而非已 pin handle，`default_runtime()` 每次从 token 的当前 snapshot 新绑定 handle，replace 返回可直接重装的旧 token；selection/execution 路径不再于请求中 `routing_snapshot.load()`（现 `selection.rs:213`、`:439`）；所有成功 snapshot publication 在同一 write lock 内从进程级 monotonic counter 分配 generation，使 legacy `add_deployment`/`remove_deployment`/`set_model_list` 以及 `add_model_alias` 的 in-place mutation 都产出新 generation 且不影响已绑定请求，alias 校验失败不递增；这些 mutation 方法**保持现有公开签名与可见性**（不在 `HD-003` 批准的 breaking 清单内，降级会超窗）；实现字段私有、只能经 `RuntimeRequestContext::validate` 构造的 request context，固定保留 validated `headers`/`timeout` 与 0.6.0-only legacy selector，且无 `Any`/字符串 dispatch；PR 限 tech D1 scope、最多 9 个非文档文件/500 changed lines，使用 `Refs #965`. Verify: `cargo test --all-features --locked core::router`; snapshot-pinning fixture 覆盖 `add_deployment` 与 `add_model_alias` —— 旧 handle 的 selection/alias/generation 不变，新直接 bind handle 与后续 `default_runtime()` 均看到新 snapshot/new generation；alias cycle error 不发布 generation；replace/rollback fixture 证明旧 token 可直接重装且 generation 单调递增；request-context struct-literal compile-fail fixture + header/timeout/selector negative tests；`rg -n "routing_snapshot.load" src/core/router` 在请求路径零命中；format/check/strict Clippy/full test；scope/overlap；exact-head independent review 与 required PR gate。
- [ ] `SP965-T011` Covers: B-006, B-012. Owner: canonical-error owner. Dependencies: SP965-T002 merged. Done when: D1E-a1 复用现有 `ErrorCode`/`CanonicalError` 与 `ProviderHttpErrorFacts`，删除 `ProviderFailureKind` 及 `src/core/providers/mod.rs` re-export；`ProviderFailureFacts::from_error` 是唯一 exhaustive `ProviderError` retry-fact match，产出 canonical code、HTTP/API status、`retry_after`、content-filter、pre-output-only、cancellation 与 modeled Bedrock 424 等非 taxonomy facts，旧 compatibility helper 也只能委托这些 facts；`src/core/router/retry_policy.rs` 只经 typed facts + `RetryContext` 决策，408/429/5xx/Bedrock 424 与 mapping 表一致；`ApiError` 只透传 400..=599，1xx/2xx/3xx/非标准值（包括现有 Gemini 200）统一映射 `ErrorCode::Internal` + HTTP 502；公开 `ErrorCode` 不新增 variant，不得新增 context-free retry API或平行 taxonomy；本 tranche 不修改 SDK conversion/redaction；PR 限 D1E-a1 的 6 文件 scope、≤500 changed lines，使用 `Refs #965`. Verify: exhaustive per-variant table fixture 同时锁定 `ErrorCode`、HTTP facts 与 `RetryPolicy::decide`，并在 `StreamRetryStage::{BeforeFirstChunk, AfterChunksEmitted}` 下锁定 `ContentFiltered`/`DeploymentError`/`Streaming`，断言 ApiError 408 retryable、200/3xx/非标准 status 不产生成功或重定向响应；`cargo test --all-features --locked utils::error`; `cargo test --all-features --locked core::router::retry_policy`; `rg -n "ProviderFailureKind" src` 零命中；source guard 只扫描 provider/retry production scope，拒绝新增或第二个 retry-fact classifier，而当前未修改的 `src/sdk/errors.rs` exhaustive SDK category match 明确延后到 T023a 删除；不得扩大该 SDK match 或新增 allowlist；scope/overlap；exact-head independent review 与 required gate。
- [ ] `SP965-T023a` Covers: B-006, B-012. Owner: canonical-error owner. Dependencies: SP965-T011 merged. Done when: D1E-a2a 在 `ProviderError` 上提供保持原 variant/category 的 `redacted()` copy，覆盖 credential、authorization/cookie header、signed URL query 与已知 secret pattern；公开且未标 `non_exhaustive` 的 `SDKError` 不新增 variant，SDK conversion 必须先 `redacted()`，再只按 canonical code 映射到既有 variant，variant selection 不得解析 string；本 tranche 不给 legacy `SDKError::ProviderError(String)` 增加真实 `#[deprecated]`，也不增加任何 `allow/expect(deprecated)`；fresh draft measurement 从含 4 行 deprecation attribute 的 491（439 additions + 52 deletions）精确减为 487（435 additions + 52 deletions），PR 精确限 `src/core/providers/unified_provider_methods.rs` 与 `src/sdk/errors.rs` 2 文件/≤500 changed lines，使用 `Refs #965`。Verify: SDK existing-category table 与 redaction negative fixture 证明 raw secret 不存在于 SDK variant/Debug/Display；ProviderError redaction fixture覆盖 message/reason/optional body fields，保留 provider/model/deployment identity 与非秘密限额；运行 tech §5 的 deterministic Python classifier command（该 command 当时不在 draft，且占 0 production/test diff line）：命令 phase-aware fail closed tokenization，T023a 要求无 decoration并输出 `phase=T023a`；T023b 只接受 `Unavailable` arm 上唯一固定 linked-followup marker +唯一局部 `#[allow(deprecated)]`，精确剥离后仍要求完整 token-shape equality；同一 lexer 定位唯一顶层 `#[cfg(test)] mod tests` item 的配对 closing brace，要求它是文件最后 item 且其后只有 whitespace（追加 comment/fn/const/macro/cfg item 均失败），只将其前缀去掉 conversion 后的完整 production comment/literal-preserving token stream 锁到由 immutable `2ff9bb2066adfb04d67b2e692ae9fbd9968fa9b5` authoring-time 生成并嵌入的 phase-specific SHA-256；T023a 禁止 outside decoration，T023b 只接受 exact enum deprecation + Gateway/`is_retryable` marker/allow synthetic baseline，运行时不读取 Git；因此既有 body/string classifier、cfg/inline/custom attr、helper/const/static/macro、comment/literal 或其他 production 变化全部失败，tests 内部 fixture/helper 明确隔离；并继续拒绝第二 `match`/`if` 与换行 `match\n redacted.to_string()`；`cargo test --all-features --locked sdk::errors`; `cargo test --all-features --locked core::providers`; `cargo clippy --all-targets --all-features --locked -- -D warnings`; diff/source check 断言无 `#[deprecated]`/`allow/expect(deprecated)` 新增；若实际 diff 超过 500 必须先减少实现且不得删除/压缩安全 fixture；scope/overlap；exact-head independent review 与 required gate。
- [ ] `SP965-T023b` Covers: B-006, B-012. Owner: canonical-error compatibility owner. Dependencies: SP965-T023a merged. Done when: D1E-a2b 给 legacy `SDKError::ProviderError(String)` 增加真实 `#[deprecated(since = "0.6.0", ...)]`，保持 0.6 公开签名、Display/retryability/构造与匹配行为；PR 非文档 scope 精确限 `src/sdk/errors.rs`、`src/sdk/client/completions.rs` 与新 `src/sdk/provider_error_deprecation_guard_tests.rs` 3 文件/≤350 changed lines，使用 `Refs #965`，不得修改 canonical mapping、redaction、control flow、错误文本、provider selection 或 sender。仅 tech §5 列名的 Gateway unavailable construction、canonical Unavailable construction、`SDKError::is_retryable` match、completion fallback、`sdk_variant` helper 和四个既有 compatibility tests 可使用逐 arm/函数/测试函数的 9 个局部 allow（`errors.rs` 8 个、`completions.rs` 1 个），每处紧邻 `SP965-T010 links 0.7 removal follow-up for SDKError::ProviderError` marker；禁止新增 crate/module-wide allow/expect、未列名 allow/expect、新 legacy callsite、value/type/import alias、wildcard、qself/raw/`Self`/split path、macro relocation 或命令行 lint 降级。`SP965-T010` 只负责创建并链接 durable handoff；全部例外与 markers 由该 0.7 follow-up 在 typed replacement/removal 落地时删除。Verify: `cargo test --all-features --locked sdk::errors`; `cargo test --all-features --locked sdk::client`; `cargo clippy --all-targets --all-features --locked -- -D warnings`; 重跑 tech §5 phase-aware classifier command并要求 `phase=T023b`；tests/completions 剩余 6 个 marker/allow 与三个 production/conversion site 一起由 `legacy_provider_error_deprecation_allowlist_does_not_grow` 的 9-site guard 精确锁定。guard 必须作为现有 final `mod tests` 内 `include!` 的 Rust-only module，support file <300 行，使用现有 `syn::Visit` + `IdentExt::unraw` 做 file/owner/role 分类并扫描全部 Rust target/source 与 lint/config/command surfaces；普通 `cargo test` 在无 `python3` 的 PATH 下通过，package/source checkout 无 `.git` 且缺少未 shipped `tests/**/*` 时仍能仅靠实际 package inventory + Cargo metadata 运行，解包 package focused test必须通过。五个当前有效 relocation/alias/path/lint/package 反例在修复前各自复现 FAIL，在修复后被拒绝或通过 portability fixture；macro owner 仍被拒绝；behavior fixture 锁定上述 0.6 行为不变；scope/overlap；exact-head independent review 与 required gate。
- [ ] `SP965-T012` Covers: B-006. Owner: canonical-error owner. Dependencies: SP965-T023b merged. Done when: D1E-b 让 Gateway typed wrapper 与真实错误响应出口消费脱敏 canonical facts —— raw `ProviderError` 仅留在 request-local retry/observability；Gateway conversion 构造 `GatewayError::Provider(e.redacted())`；`src/utils/error/gateway_error/response.rs` 不再用 raw `provider_error.to_string()` 拼装 message，`src/server/routes/ai/openai_errors.rs` 改用同一 `http_facts()`/`canonical_code()`；HTTP response 的 `retryable` 仅是 grandfathered `CanonicalError::canonical_retryable` presentation hint，不得回流为 runtime retry/fallback 决策；PR 限 D1E-b scope、最多 4 个非文档文件/500 changed lines，使用 `Refs #965`. Verify: `cargo test --all-features --locked utils::error::gateway_error`; redaction negative fixture 证明 credential/authorization/signed query 不存在于 Gateway variant、Debug/Display 或响应体；source guard 断言 Gateway conversion/响应出口不保存 raw error、无 `to_string()` 分类，且 routing/retry 代码不消费 presentation hint；repository gates、scope/overlap、review/gate。
- [ ] `SP965-T017` Covers: B-006, B-011. Owner: retry-compatibility owner. Dependencies: SP965-T012 merged. Done when: D1E-c 给现有 `ProviderError::is_retryable`、`ContextualProviderError::is_retryable`、`ProviderErrorTrait::is_retryable`、`SDKError::is_retryable`、`core::router::execution::is_retryable_error`、`ErrorUtils::should_retry` 统一添加 0.6.0 deprecation，保持现有返回行为以保证源码兼容，但 production provider routing/retry path 不再调用它们；`batches.rs`/`fine_tuning.rs` 的 provider retry 决策迁到 `RetryPolicy::decide`；六个 helper 全部列入 T010 的 0.7 removal handoff。`ErrorCode::is_retryable`/`CanonicalError::canonical_retryable` 明确 grandfather 为 A2A/MCP/HTTP presentation coarse fact，0.6/0.7 不删除且不得参与 provider retry/fallback；PR 限 D1E-c scope、最多 20 个非文档文件/500 changed lines（T017 file-budget amendment 2026-07-21，见 Handoff Notes (8)），使用 `Refs #965`. Verify: compatibility fixture 锁定六个 deprecated helper 的 0.6 返回值；production routing/retry source guard 对六个 helper call site 零命中（定义/tests/compat fixture allowlist），并允许 A2A/MCP/response serialization 对 grandfathered canonical helpers 的现有调用；`RetryPolicy::decide` focused tests；repository gates、scope/overlap、review/gate。
- [ ] `SP965-T003` Covers: B-002, B-003, B-004, B-005, B-006, B-011. Owner: completion-unary facade owner. Dependencies: SP965-T017 merged；2026-07-21 D2 typed-error amendment reviewed/merged. Done when: D2 让 free functions 与批准保留的 completion facade 通过 canonical runtime 执行 unary 请求，旧 env/registry/static selector 不再是 fallback；`RuntimeHandle` 以 crate-private typed API 在同一个 pinned snapshot 内执行 selection/retry/fallback 并直接返回 terminal `ProviderError`，不得经 `RouterError` 往返压扁；现有 RouterError compatibility API 只在最外层委托 typed implementation 后映射一次；unary adapter 不做 textual provider-prefix gate，runtime alias 先解析，surface unsupported 由 selected provider 返回 typed error；不得复制 routing loop、暴露 router accessor或使用 adapter error side-channel，公共行为遵循 `HD-003/004`。PR 限 D2 amended scope（候选增加 `src/core/router/execute_impl.rs`）、实际最多 8 个非文档文件/500 changed lines，使用 `Refs #965`. Verify: `cargo test --all-features --locked core::completion`; focused fixtures 证明 prefix-looking runtime alias 可达、terminal authentication/timeout/API/unsupported/cancel/parsing/content-filter 中至少一类保持原 variant/canonical code并经 Gateway redaction、source guard 拒绝 `unsupported_explicit_completion_selector`/`router_error_to_provider_error`/legacy fallback；`cargo test --all-features --locked --test lib integration::router_tests`; compile/doc compatibility fixtures；repository gates、scope/overlap、review/gate。
- [ ] `SP965-T013a` Covers: B-006. Owner: credential-compare owner. Dependencies: SP965-T003 merged. Done when: D3Ca 让 credential 比较不再走 `utils::auth::crypto::hmac::constant_time_eq` 的变长路径 —— construction boundary 仅在能证明 stored credential 与实际 provider credential 完全相同时预计算定长 SHA-256 digest（复用 `Cargo.toml:97` 既有 `sha2`，不新增依赖），以不可序列化、`Debug` redacted 的 `[u8; 32]` 可选 metadata 随 immutable routing snapshot 保存且不保留 raw credential；空值、provider alias/settings/env fallback、同一 config 的冲突 credential source 或任一来源不明确时不得发布 metadata、hash 空值或猜测 credential；`set_model_list`、普通 replacement/add 在没有 fresh provenance 时必须清除旧 metadata，禁止仅凭 deployment ID 继承 digest；metadata 缺失时 legacy selector typed fail closed；request selector 每请求只计算一次 digest，candidate loop 只做定长 32 字节 constant-time compare，禁止逐 candidate hash 变长 stored secret；`verify_hmac_signature` 等既有 HMAC 调用方行为不变；PR 限 D3Ca scope、最多 5 个非文档文件/500 changed lines，使用 `Refs #965`. Verify: `cargo test --all-features --locked utils::auth::crypto`; router fixture 证明不同长度 credential 只以 fixed digest 存储、request hash count 恒为一次且零/一/多匹配 fail-closed，并覆盖空/未知 provenance 与 Cloudflare 顶层 `api_key`/`settings.api_token` shadow conflict 不发布 metadata、same-ID rotation 清除旧 digest；保留并通过现有 `test_constant_time_eq_different_lengths`/`test_constant_time_eq_empty_strings` 语义（不得弱化断言）；source guard 拒绝 snapshot metadata 的 raw secret/`Display`/serde derive；repository gates、scope/overlap、review/gate。该 tranche 触及 crypto，按 SEC-11 需人工安全 review。
- [ ] `SP965-T013b` Covers: B-006. Owner: credential-provenance owner. Dependencies: SP965-T013a merged. Done when: D3Cb 在 canonical provider construction/config normalization boundary 一次性解析 provider 实际使用的 effective credential，把同一份 normalized config 交给 `create_provider`，且只从成功构造 provider 的该 credential 发布 digest；精确覆盖共同的 native 顶层 `api_key` > `settings.api_key` precedence、代表性 provider alias 与其 canonical provider 同结果、catalog primary/alternate env、Cloudflare `api_token`、Replicate `api_key`/`api_token`/env、FalAI、Cohere、Gemini `api_key`/`google_api_key`/`gemini_api_key`/env 的既有 precedence；selector 不得读取 env、重扫 config、调用 factory 或构造 provider；空白、未知或无法证明 provenance 时继续无 metadata 并 typed fail closed；PR 限 D3Cb scope、最多 3 个非文档文件/500 changed lines，使用 `Refs #965`. Verify: actual `Router::from_gateway_config` fixtures 锁定上述 provider credential precedence、top-level/settings shadowing、代表性 alias selectors、空白值、unknown provider 与 RAII env restoration；source guard 证明 resolver 无 env/config/factory access；repository gates、scope/overlap、review/gate。该 tranche 改变 credential provenance，按 SEC-11 需人工安全 review。
- [ ] `SP965-T004` Covers: B-003, B-004, B-006, B-008, B-010, B-011. Owner: completion-stream-and-override owner. Dependencies: SP965-T013b merged. Done when: D3 按已解决的 `HD-002` 保留 validated `headers`/`timeout`，将 `api_key`/`api_base` 限制为 0.6.0 deprecated legacy selector 并锁定 0.7.0 removal；按 tech spec §2 的 override 分类表，`api_version`/`organization` 同样降为 0.6.0 deprecated legacy selector match 维度，不再经 `router_impl.rs:215-225` 写入 `*_override` dynamic provider config，也不得静默丢弃；`src/core/completion/types.rs` 上的四个字段带 `#[deprecated(since = "0.6.0", ...)]`；`LegacyRuntimeSelector` 的手写 `Debug` 对 present `api_key` 与 `api_base` 一律输出 `[REDACTED]`，不得泄漏 URL userinfo/query/private endpoint；stream 由 canonical selected lease 执行并区分 pre-output failure/post-output failure/cancel/success，legacy dynamic provider/client path 不再可达；PR 限 D3 scope、最多 8 个非文档文件/500 changed lines，使用 `Refs #965`. Verify: `cargo test --all-features --locked streaming`; focused override/security negative tests 覆盖 `api_version`/`organization` 的零匹配/多匹配 fail-closed，并断言 raw key、signed `api_base` query 与 userinfo 不出现在 selector Debug/Display/error/log capture；`rg -n "api_version_override|organization_override" src` 零命中；completion source guard；repository gates、scope/overlap、review/gate。
- [ ] `SP965-T005` Covers: B-001, B-002, B-004, B-005, B-006, B-007, B-008, B-009, B-011. Owner: SDK runtime-binding owner. Dependencies: SP965-T004 merged. Done when: D4 将 `ClientConfig` 归一到 canonical runtime，SDK selection 不再用本地 stats/临时 `RoutingContext` 决策，保留 facade 遵循 `HD-001/003/004`；`LLMClient::from_runtime` 收并长期持有 `RuntimeBinding` 而非 `Arc<UnifiedRouter>`/`RuntimeHandle`，`runtime()` 返回该 refreshable binding，每个公开 operation 入口恰好 bind 一次并全程委托所得 handle；同 generation conformance 只能使用显式命名、request-scoped 的 internal `*_with_runtime_handle` adapter，该 adapter 不得把 handle 存回 client；不得为 SDK generation 伪造值或填 sentinel；PR 限 D4 scope、最多 7 个非文档文件/500 changed lines，使用 `Refs #965`. Verify: `cargo test --all-features --locked sdk::client`; fixture 证明旧 operation handle 在 mutation 后仍 pinned，而同一 client 的下一 operation 通过 binding 看到 new generation；显式 fixed-handle adapter 与 HTTP fixture 使用同一 handle 时 generation identity 相等，且 client struct 不含 `RuntimeHandle`；SDK config/error compatibility fixtures；repository gates、scope/overlap、review/gate。
- [ ] `SP965-T006` Covers: B-002, B-003, B-005, B-006, B-007, B-008, B-009, B-010. Owner: SDK execution owner. Dependencies: SP965-T005 merged. Done when: D5 删除 SDK provider-type sender、local retry/selection state 与双重 stats truth，chat/stream/embeddings 从 canonical selected provider/state 派生；PR 限 D5 scope、最多 7 个非文档文件/500 changed lines，使用 `Refs #965`. Verify: focused SDK chat/stream/embeddings + exactly-once tests；SDK source guard；`cargo test --all-features --locked sdk`; repository gates、scope/overlap、review/gate。
- [ ] `SP965-T007` Covers: B-001, B-003, B-011. Owner: registry-compatibility owner. Dependencies: SP965-T006 merged. Done when: D6 迁移 completion/embedding 等全部 production `ProviderRegistry` 调用方，使 canonical runtime 与任何执行路径都不再读写它；按 `HD-003` 给 `DefaultRouter`、completion `Router` trait（在其定义处 `src/core/completion/router_trait.rs`，而非只在 `completion/mod.rs` 的 re-export 上）与 `ProviderRegistry` 打 `#[deprecated(since = "0.6.0", ...)]`；`ProviderRegistry` **保持现有签名与 0.5.x 行为**，不改造成 stateless facade —— 现有 `register(&mut self, ..) -> ()`（`provider_registry.rs:22`）、`clear(&mut self)`（`:72`）、`remove(&mut self, ..) -> Option<Provider>`（`:52`）没有错误通道，"mutation 返回 configuration error"要么静默 no-op（违反 U-29）要么需要 `HD-003` 推迟到 0.7.0 的 breaking 签名变更；`DefaultRouter` 只包装 refreshable binding source：显式模式持有 `RuntimeBinding`，process-default 模式每次 trait call 重新取得 default handle，两者都只在 call 入口 bind/load 一次，不保存 `RuntimeHandle`；0.7.0 整体删除留给 T010 创建并链接的 follow-up；PR 限 D6 scope、最多 7 个非文档文件/500 changed lines，使用 `Refs #965`. Verify: `rg -n "ProviderRegistry" src` 在 production 代码（只排除 `provider_registry.rs` 自身定义、`src/core/providers/mod.rs`/`src/lib.rs` 兼容 re-export 与 tests）零命中，这是 D6 的完成信号；guard 证明 allowlisted re-export 不构造/读取/mutate registry；fixture 证明同一 `DefaultRouter` 在 `replace_default_runtime` 后的下一 call 观察新 generation，已开始的 call 仍 pinned；`rg -n "DefaultRouter|dyn Router" src`; approved allowlist/source guard；compile/doc fixtures 证明 0.5.x registry 调用方仍可编译（仅有 deprecation warning）；repository gates、scope/overlap、review/gate。
- [ ] `SP965-T014` Covers: B-001, B-002, B-003, B-009, B-010. Owner: HTTP runtime-binding owner. Dependencies: SP965-T007 merged; #1026 merged/closed prerequisite is already satisfied. Done when: D7a 让 server startup/AppState 持有 canonical `RuntimeBinding` 而非已 pin handle，在每个请求入口恰好 bind 一次 `RuntimeHandle`，并为 `execution.rs`/`budgeted.rs` 增加 handle-aware unary/stream helper；旧 `&UnifiedRouter`/`Arc<UnifiedRouter>` wrapper 仅为跨 tranche build-green 暂留，禁止新 caller；`provider_selection.rs` 迁入同一 binding；PR 限 D7a scope、最多 6 个非文档文件/500 changed lines，使用 `Refs #965`. Verify: focused execution/budget/retry-delay tests；同一 `AppState` 的 in-flight request 在 snapshot publication 后仍 pinned，publication 后开始的下一 request 观察 new generation；source guard 拒绝 AppState/长期 server object 存 `RuntimeHandle` 并只 allowlist 暂留 wrapper；format/check/strict Clippy/full test；scope/overlap；exact-head review/gate。
- [ ] `SP965-T015` Covers: B-002, B-003, B-004, B-005, B-006, B-008, B-009. Owner: HTTP unary-caller owner. Dependencies: SP965-T014 merged. Done when: D7b 将 audio/chat/embeddings/images/moderations/rerank/Gemini unary callers 全部改传请求入口 bind 的 pinned handle，不再从 AppState 直接借用当前 router；`gemini.rs` 只做 binding plumbing，#1026 的 wire、endpoint policy 与 selected identity 不变；PR 限 D7b scope、恰为最多 10 个非文档文件/500 changed lines，使用 `Refs #965`. Verify: focused HTTP unary tests + `tests/gemini_sdk_routes/runtime_provider_tests.rs` 原样通过；source guard 对 D7b callers 的 `state.unified_router` 零命中；repository gates、scope/overlap、review/gate。
- [ ] `SP965-T016` Covers: B-002, B-003, B-005, B-006, B-007, B-008, B-009, B-010. Owner: HTTP stream/query cleanup owner. Dependencies: SP965-T015 merged. Done when: D7c 将 stream、models、response cache 与 Gemini provider callers 迁到 pinned handle，删除 AppState 的 public router field和 D7a 暂留 legacy helper；`gemini.rs`/`gemini/provider.rs` 仅 binding plumbing，行为边界与 #1026 tests 不变；production HTTP 不再直接调用 `UnifiedRouter` selection/execution，也不在请求中 load 新 snapshot；PR 限 D7c scope、最多 10 个非文档文件/500 changed lines，使用 `Refs #965`. Verify: HTTP stream/models/cache/Gemini focused tests；`rg -n "state\.unified_router|Arc<UnifiedRouter>|&UnifiedRouter" src/server/routes` 仅批准 construction/类型定义 allowlist；snapshot replacement 与 post-output/cancel fixtures；repository gates、scope/overlap、review/gate。
- [ ] `SP965-T018` Covers: B-001, B-002, B-009, B-010. Owner: HTTP background-response owner. Dependencies: SP965-T016 merged. Done when: D7d 在 `background=true` response 被接受、spawn 发生前恰好 bind 一次 `RuntimeHandle`，spawned task 与其 chat execution helper 只消费该 pinned handle，不得从 `AppState`/`RuntimeBinding` 再 bind 或 load snapshot；同步 response 行为不变；PR 限 D7d scope、最多 4 个非文档文件/500 changed lines，使用 `Refs #965`. Verify: lifecycle fixture 在 accept 后、task 真正执行前发布新 snapshot，证明 background task 仍使用 accepted generation，而随后请求看到新 generation；source guard 拒绝 spawned closure 捕获 refreshable binding/AppState 进行 selection；focused responses/chat tests；repository gates、scope/overlap、review/gate。
- [ ] `SP965-T019` Covers: B-002, B-007, B-008, B-009. Owner: management-attempt-plan owner. Dependencies: SP965-T018 merged. Done when: D7e 在 pinned `RuntimeHandle` 上增加 crate-private capability-only attempt plan，供没有 model 的 lifecycle operation 使用；候选只来自该 handle 的 immutable snapshot，使用 capability-derived stable routing key并复用 canonical health/cooldown/budget/routing strategy、`RetryPolicy::decide` 与 exactly-once lease settlement，禁止空字符串/sentinel model、route config scan 或 adapter provider loop；有 model 的 create operation 继续使用既有 model + capability selection；PR 限 D7e scope、最多 4 个非文档文件/500 changed lines，使用 `Refs #965`. Verify: router fixture 覆盖同 capability 跨 model deployment 的 deterministic selection/retry/fallback、旧 handle snapshot isolation、new handle generation、cancel/settlement，并证明不支持 capability 时 typed fail closed；source guard 拒绝 sentinel model 与第二 routing loop；repository gates、scope/overlap、review/gate。
- [ ] `SP965-T020` Covers: B-001, B-003, B-005, B-006. Owner: provider-management-dispatch owner. Dependencies: SP965-T019 merged. Done when: D7f 引入 crate-private closed management operation/result（batch create/list/get/cancel 与 fine-tuning lifecycle），只允许 D7e selected deployment 的 provider 执行；ID 是独立字段并由 provider 做 URL-segment encoding，调用方不得传任意 URL/path/method 或取得 raw client；OpenAI/OpenAI-compatible 实现复用其构造时 auth、endpoint policy 与 pool，并仅声明实际可执行 capability，其他 provider typed `NotSupported`；PR 限 D7f scope、最多 9 个非文档文件/500 changed lines，使用 `Refs #965`. Verify: provider-level local-listener fixtures 覆盖 auth/endpoint ownership、所有 closed operation、encoded adversarial ID、unsupported provider、redacted error 与 capability declaration；source guard 拒绝 public management dispatch、raw client escape 和 arbitrary method/path；repository gates、scope/overlap、security review/gate。
- [ ] `SP965-T021` Covers: B-002, B-003, B-005, B-006, B-007, B-008, B-009. Owner: HTTP batch-runtime owner. Dependencies: SP965-T020 merged. Done when: D7g 将 batch create/list/get/cancel route 改为只组装 typed operation并委托 D7e/D7f runtime plan；route 不再扫描 `config.gateway.providers`、构造 `RouteHttpClient`、循环 provider 或自行 retry/fallback，wire 行为保持；PR 限 D7g scope、最多 2 个非文档文件/500 changed lines，使用 `Refs #965`. Verify: batch route fixtures 覆盖 selected identity、model-aware create、capability-only lifecycle、runtime retry mapping 与 wire regression；source guard 对 config provider scan、route-owned client/sender、provider loop 零命中；repository gates、scope/overlap、security review/gate。
- [ ] `SP965-T022` Covers: B-002, B-003, B-005, B-006, B-007, B-008, B-009. Owner: HTTP fine-tuning-runtime owner. Dependencies: SP965-T021 merged. Done when: D7h 将 fine-tuning lifecycle route 改为只组装 typed operation并委托 D7e/D7f runtime plan，不再扫描 config、构造 `OpenAIFineTuningProvider`、循环 provider 或自行 retry/fallback；最后调用方迁走后删除 route-owned `route_http.rs`/`provider_config.rs` helper 与 module export，wire 行为保持；PR 限 D7h scope、最多 5 个非文档文件/500 changed lines，使用 `Refs #965`. Verify: fine-tuning fixtures 覆盖 model-aware create、capability-only lifecycle、selected identity、runtime retry/error mapping 与 wire regression；source guard 对全部 production AI routes 的 config provider scan、`RouteHttpClient`、`OpenAIFineTuningProvider`、adapter-owned sender/provider loop 零命中；repository gates、scope/overlap、security review/gate。
- [ ] `SP965-T008` Covers: B-001, B-002, B-003, B-004, B-005, B-006, B-007, B-008, B-009, B-010, B-011, B-012. Owner: conformance owner. Dependencies: SP965-T022 merged. Done when: D7i 只在 integration test scope 内完成三入口 deterministic conformance；同一 fixtures 验证 selection identity、errors、retry/fallback、snapshot、stream/cancel、exactly-once，source guard 扫描全部 production AI routes 并 red/green，明确覆盖 background response、batch 与 fine-tuning ownership；PR 最多 3 个非文档文件/500 changed lines，使用 `Refs #965`，不得在 0.7 removal handoff 缺失时提前关闭 issue. Verify: `cargo test --all-features --locked --test lib integration::router_runtime_conformance`; source guard 证明 config provider selection scan、route-owned HTTP/provider construction/provider loop 与 spawned-task rebind 零命中；format/check/strict Clippy/full test；scope/overlap；exact-head implementation/security review、CI、0 unresolved threads、required gate。
- [ ] `SP965-T010` Covers: B-006, B-011. Owner: release-policy owner + spec coordinator. Dependencies: SP965-T008 merged；0.6.0 deprecation symbols/fields 与 migration note 已发布或有可验证的 release artifact. Done when: 只创建并链接独立 0.7.0 removal issue/spec，不在 T010 中修改或删除 production/test compatibility 站点；该 durable follow-up 逐项列出 `DefaultRouter`、completion `Router` trait、`ProviderRegistry`（0.7.0 整体删除该类型）、request-level `api_key`/`api_base`/`api_version`/`organization`、legacy `SDKError::ProviderError(String)` 的 typed replacement/removal，以及 D1E-c 的六个 deprecated provider retry helpers（`ProviderError`/`ContextualProviderError`/`ProviderErrorTrait`/`SDKError`/router execution/`ErrorUtils`）；明确记录 0.6 未给公开 `ErrorCode`/`SDKError` 增加 variant，0.7 typed SDK 设计须单独审查 exhaustive-match breaking impact；`ErrorCode::is_retryable`/`CanonicalError::canonical_retryable` 已 grandfather、0.7 不删除且不得被 provider runtime 消费；该 follow-up 将 `.github/workflows/version-bump.yml` 修订与 deterministic 0.x fixture 设为 removal 前硬依赖，并禁止用 non-breaking commit label 绕过；#965 closure comment 链接该 durable work item. Verify: GitHub issue/spec link；0.6.0 release/deprecation evidence；人工核对 removal 与 grandfather 表完整 —— source-of-truth 是 B-011/`HD-003`（`DefaultRouter`、`Router` trait、`ProviderRegistry` ownership）**加上** B-006/`HD-004`/D1E-a1/a2a/a2b/b/c（SDK legacy variant、六个 removal helper、两个 grandfather helper）**加上** tech spec §2 override 分类表（`api_version`/`organization`）；仅比对 B-011/HD-003 会漏掉 SDK error、retry helper 与 override 字段。
- [ ] `SP965-T009` Covers: B-001, B-002, B-003, B-004, B-005, B-006, B-007, B-008, B-009, B-010, B-011, B-012. Owner: closure auditor. Dependencies: SP965-T008 merged；SP965-T010 durable 0.7.0 follow-up 已创建并链接. Done when: merged `origin/main` 证据证明全部 runtime invariants、HD 决策与 tranche merge chain，required gate PASS；canonical runtime 与全部 production execution path 不再消费 `ProviderRegistry`，但批准保留至 0.7.0 的 deprecated、独立可变 compatibility type 仍保持 0.5.x 行为；#965 closed、分支删除，#519 仅保留 roadmap，closure audit 无遗留双 runtime claim；0.7.0 public removal 由 SP965-T010 创建并链接的 follow-up 实施并关闭. Verify: GitHub evidence adapter；offline `pr_gate.py --mode required`；merged SHA/issue state/reviewThreads/CI checks；`rg -n "ProviderRegistry" src` 在定义/re-export/tests 外零命中，其他 source guard 在 `origin/main` fresh checkout 重跑。

## 并行拆分

- T001 → T002 → T011 → T023a → T023b → T012 → T017 → T003 → T013a → T013b → T004 → T005 → T006 → T007 → T014 → T015 → T016 → T018 → T019 → T020 → T021 → T022 → T008 严格串行：后续任务必须从前一
  tranche 的 merged `origin/main` 开始；禁止并行写 completion、SDK、error adapter 或 runtime files。
  对应 tranche 依次为 D0 → D1 → D1E-a1 → D1E-a2a → D1E-a2b → D1E-b → D1E-c → D2 → D3Ca → D3Cb → D3 → D4 → D5 → D6 →
  D7a → D7b → D7c → D7d → D7e → D7f → D7g → D7h → D7i。
- T010 在 T008 后建立 durable release handoff；T009 只读 closure/review lane 必须等 T010 完成，可与
  coordinator 的 final exact-head verification 并行；reviewer 不写文件。
- 每个 writable task 使用独立 worktree 和单一 owner。若需要多 agent，只能按 tech 表中的 disjoint file
  ownership 拆只读 reviewer lane；不得让两个 agent 修改同一路径。
- merged PR #1026 是已满足前置；`tests/gemini_sdk_routes/runtime_provider_tests.rs` 保持只读。D7b/D7c 对
  `src/server/routes/ai/gemini.rs`、`src/server/routes/ai/gemini/provider.rs` 只允许 binding plumbing，禁止改变
  wire、endpoint policy 或 selected identity，且必须让上述只读 regression suite 原样通过。
- 任一 tranche 超过 10 个非文档文件或 500 changed lines，先提交并合并 spec amendment 拆 tranche；禁止
  删除测试、压缩断言或扩大 allowlist 规避 gate。

## 验证

- Product、tech mapping 与 tasks `Covers:` union 精确为 B-001 至 B-012，无 orphan/undeclared invariant。
- `HD-001` 至 `HD-004` 在 implementation 前全部 resolved；current packet 不把任何 option 当默认。
- 每个 tranche fresh exact-head 通过 `cargo fmt --all -- --check`、
  `cargo check --all-targets --all-features --locked`、strict Clippy、
  `cargo test --all-features --locked -- --test-threads=1`。
- 每个 PR 有 scope/overlap、independent review、CI、0 unresolved review threads 与 required gate 证据。
- final conformance 与 source guard 在 merged `origin/main` 重跑；support matrix 单测不能替代 B-012。

## Handoff Notes

- `HD-001` 至 `HD-004` 已由 issue comment `4982855807` 解决；implementation 只在 T001 amendment review/merge 后开放。
- canonical runtime 是现有 `UnifiedRouter` deployment snapshot；生命周期/API 以本 amendment 的 resolved contract 为准。
- 绝不恢复 config rescan、临时 provider、adapter-owned reqwest client、SDK local routing stats 或
  `DefaultRouter` fallback。
- #725/#728 是已完成前置；#966/#1026、#968 是相邻行为边界。GH-965 不修改其 wire、support matrix、
  endpoint policy 或 Gemini selected-provider contract；D7b/D7c 仅迁移 binding 类型。
- D1-D7i 只用 `Refs #965` 并保持 issue open；T010 durable release handoff 完成后才由 T009 closure audit
  关闭 #965。每次 merge 后删除阶段分支并审计下一 tranche base SHA。
- 0.6.0 只做 deprecation；T010 只创建并链接 durable handoff，0.7.0 removal 由该 follow-up 实施。当前 version-bump workflow 会把
  0.x breaking 计算为 1.0.0，未先修订并 fixture 验证时不得执行 removal；不得通过提交命名规避。
- 本 amendment 在 review 后新增的判定（均不改变 `HD-001`~`HD-004` 已批准的决策与 0.6→0.7 窗口）：
  (1) generation identity 由 `RuntimeHandle` pin 住的 `Arc<RoutingSnapshot>` 承担，process default 存
  `RuntimeBinding` token 并在 load 时重新 pin 当前 snapshot，replace 返回 token 支持 rollback，所有 deployment/
  alias snapshot mutation 方法保持公开签名不变且成功 publication 必须递增 generation；(2) 不新增 context-free retryability API，retry 收敛到既有
  `RetryPolicy::decide`，T011 删除 `ProviderFailureKind` 并由唯一 exhaustive match 保留 typed facts，T023a 再完成 provider redaction/SDK mapping，T023b 独立增加 legacy SDK error deprecation 与全 Rust target/source compatibility guard；六个 provider-specific
  bool helper 在 0.6.0 deprecated/production 零消费，0.7.0 删除，两个 canonical coarse helper 明确 grandfather；
  (3) Gateway 既有 typed wrapper 只持有 redacted copy；0.6 SDK 先脱敏再映射到既有 variant，不新增公开 variant；长期 SDK/HTTP/completion facade 持有
  refreshable `RuntimeBinding`，每个 operation/request 入口只 bind 一次，只有显式 request-scoped conformance
  adapter 可接收 fixed `RuntimeHandle`；
  (4) `ProviderRegistry` 在 0.6.0 保持现有签名与行为，D6 只切断所有权；(5) `api_version`/`organization`
  与 `api_key`/`api_base` 同类处理；(6) credential 在 deployment publication 时预计算定长 digest，request path
  每请求只 hash 一次后比较；(7) HTTP binding/route ownership 按 D7a-D7i 分 tranche 完成，Gemini 只允许 binding plumbing，background task 在 acceptance 时 pin handle，无 model 的 lifecycle operation 使用 pinned snapshot 的 capability-only runtime attempt plan，batch/fine-tuning 只通过 selected runtime provider 的 closed management dispatch。
  任一项若维护者不同意，需在实现前修订本 packet，实现者不得自行改选。
  (8) T017 file-budget amendment（2026-07-21，理由按独立 review 实测证据修订）：T017 非文档文件上限由
  「最多 8 个」修订为「最多 20 个」，500 changed lines 上限及其余约束不变。实测基础（review lane 在 pinned
  toolchain rustc/clippy 1.96.1 上以最小 crate 实测 + 全仓库 rg 扫描）：rustc 的 `#[deprecated]` 对 inherent
  方法与自由函数在同 crate 使用点**不产生 warning**（只有 deprecated trait 方法在同 crate 调用点报 warning；
  六个 helper 中恰一个是 trait 方法 `ProviderErrorTrait::is_retryable`，且全仓库无解析到它的同 crate 调用点）。
  因此文件散布不是编译器 warning 驱动，而是以下原子耦合的完成信号驱动：(a) production provider routing/retry
  path 零消费要求 source guard 零命中扫描覆盖散布于 bedrock/openai_like/sdk/router/utils/server routes 的全部
  production 调用点迁移；(b) T023b 式「marker + 局部 allow + guard 精确锁定」约定要求新增 allow 站点登记
  `expected_attrs`（已合并 guard 对全部 Rust target/source 执行全仓库精确匹配，未登记站点一律 FAIL）；
  (c) batches/fine_tuning 迁到 `RetryPolicy::decide`；(d) 六个 helper 的 0.6 返回值锁定 fixtures。拆成多个 PR
  会产生「部分 helper 已弃用、guard 半登记、production 消费半迁移」的中间态，无法被 exact-head 精确匹配表达，
  且违反「禁止删除测试、压缩断言或扩大 allowlist 规避 gate」。故不拆分，只放宽文件数上限。新增 allow 站点归入
  done-when 既有类别——定义（六个 helper 自身 attribute 与定义侧委托）、grandfathered 委托
  （`CanonicalError::canonical_retryable` presentation 调用）、tests 与 compat fixture——且每处由 T023b 的
  deprecation guard `expected_attrs` 精确锁定（透明增长，仍为精确锁定）。预算明细（实测 18 文件）：6 个
  helper 定义文件、1 个定义侧委托、2 个 production 迁移、1 个 grandfathered 委托、6 个 test 模块/站点 allow、
  1 个 guard 登记、1 个新增 D1E-c 测试文件。production provider routing/retry path 零消费、batches/fine-tuning
  迁到 `RetryPolicy::decide` 的完成信号不变。本 amendment 不改变 `HD-001`~`HD-004` 任何决策、0.6→0.7 窗口与
  串行 tranche 顺序。
  (9) T003 typed-error amendment（2026-07-21）：PR #1098 exact head `6e714099` 的 independent review 证明
  D2 原 allowlist 无法同时满足 pinned generation 与 `ProviderError` terminal facts 保真。批准的修订只在
  `src/core/router/execute_impl.rs` 增加 crate-private pinned typed boundary，并要求现有 RouterError API 外层委托；
  completion unary 删除 runtime alias 前的 textual prefix gate。候选路径从 8 增至 9，但实际 PR 上限仍为
  8 个非文档文件/500 changed lines；禁止 adapter side-channel、第二 routing loop 或公开 router accessor。
  (10) T013 credential-provenance split amendment（2026-07-21）：exact-head review 证明同时交付 fixed digest、
  replacement cleanup、全部 provider credential precedence 与不压缩的安全 fixture 的最小 draft 为
  547 changed lines（535 additions + 12 deletions）。#1100 已提交 core digest 部分为 389 changed lines，
  provenance draft 相对该 head 为 432 changed lines。依既有超限规则拆为严格串行 T013a/T013b；不得删测试、
  压缩断言或临时扩大 500-line gate。T013a 中无法证明 provenance 的 deployment 不发布 metadata 并 typed
  fail closed；T013b 完成 canonical provider credential normalization 后 T004 才可开始。本拆分不改变
  `HD-001`~`HD-004`、0.6→0.7 窗口或产品行为。
